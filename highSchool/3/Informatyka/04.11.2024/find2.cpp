#include <iostream>
#include <string>
#include <map>

using std::cin;
using std::cout;
using std::endl;
using std::string;
using std::map;

int main(void) {
    map<string, size_t> strings;

    while (true) {
        string str;
        cin >> str;

        if(str == "***") break;
        else {
            
        }
    }
    
}